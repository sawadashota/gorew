version: 2
jobs:
  build:
    docker:
      - image: circleci/golang:1.10.0
    working_directory: /go/src/github.com/sawadashota/gocmd
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: go get -v -t -d ./...
      - run:
          name: Create binary
          command: go install
      - run:
          name: Test
          command: go test -v ./...
      - run:
          name: Create svg from gocomands/cpu.pprof
          command: go tool pprof -svg ./pprof/gocomands/cpu.pprof > ./pprof/gocomands/cpu.svg
      - run:
          name: Create svg from maindir/cpu.pprof
          command: go tool pprof -svg ./pprof/maindir/cpu.pprof > ./pprof/maindir/cpu.svg
      - store_artifacts:
          path: /go/src/github.com/sawadashota/gocmd/pprof
